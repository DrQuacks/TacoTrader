[project]
name = "taco-trader-python"
version = "0.1.0"
description = "Python bridge utilities for TacoTrader market data providers."
requires-python = ">=3.13"
dependencies = [
  "yfinance>=1.3.0",
]

{
  "name": "taco-trader",
  "version": "0.1.0",
  "description": "TACO Trader app for evaluating Trump-driven market dips and rebound setups.",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.server.json && tsc -p tsconfig.client.json",
    "start": "npm run build && node dist/server.js"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.6.0",
    "@types/three": "^0.184.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "three": "^0.184.0"
  }
}
